name: Birthday reminders
on:
  push
  # schedule:
  #   - cron: "* * * * *"
jobs:
  birthday_reminders:
    steps:
      - name: send reminders
        uses: "node16"
        main: "jobs/birthday_reminders.tsx"
